def is_palindrome(text: str) -> bool:
    """Return True when the input reads the same forward and backward.

    Method requirements:
    - Ignore spaces, punctuation, and letter casing.
    - Treat an empty string as a palindrome.
    """